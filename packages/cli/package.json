{
  "name": "@jssnippets/cli",
  "version": "1.0.0",
  "license": "MIT",
  "description": "",
  "files": [
    "dist"
  ],
  "bin": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "dependencies": {
    "@jssnippets/client": "^0.1.0"
  },
  "devDependencies": {
    "@jssnippets/server": "^1.0.0",
    "commander": "^8.3.0",
    "@types/node": "^17.0.5",
    "esbuild": "0.8.26",
    "typescript": "^4.5.4"
  }
}
